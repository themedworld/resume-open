import { Resume } from 'src/resume/entities/resume.entity';
export declare class ResSet {
    id: number;
    themeColor: string;
    fontSize: string;
    documentSize: string;
    resume: Resume;
}

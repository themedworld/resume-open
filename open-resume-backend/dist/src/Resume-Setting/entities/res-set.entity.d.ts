import { Resume } from 'src/resume/entities/resume.entity';
export declare class ResSet {
    id: number;
    themeColor: string;
    fontFamily: string;
    documentSize: string;
    fontSize: string;
    resume: Resume;
}
